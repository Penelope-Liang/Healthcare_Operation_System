# Legacy Pages

These files are preserved for reference to the original coursework flow.

The current application does not link to these pages, and Apache access is blocked by `.htaccess` because the legacy files do not follow the same validation and prepared-statement patterns as the main application pages.
